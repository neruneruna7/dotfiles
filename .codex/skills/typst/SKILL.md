---
name: typst
description: Typstで文章を書くときのコツを適用する
---
- `typst compile <filename>` でtypstのファイルをコンパイルできる
  - typstのファイル編集後は，コンパイルが通ることを確認せよ．

Typstソースを生成・修復する場合,TeX,LaTeX,Markdown,Typstの記法を混同するおそれがあるときは,必ず `references/tex-md-typst-diff.md` を参照する.
